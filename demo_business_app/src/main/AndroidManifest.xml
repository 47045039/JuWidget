<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ju.demo.business">

    <!-- 此处定义的permission，必须和业务aar里使用的permission一致 -->
    <permission
        android:name="com.ju.widget.permission.WIDGET_BUSINESS.com.ju.demo.business"
        android:protectionLevel="normal" />

    <!-- 此处使用的permission，必须和widget框架里定义的WidgetService permission一致 -->
    <uses-permission android:name="com.ju.widget.permission.WIDGET_SERVICE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.ju.demo.business.DemoBusiness"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:permission="com.ju.widget.permission.WIDGET_BUSINESS.com.ju.demo.business">
            <intent-filter>
                <action android:name="com.ju.widget.intent.action.WIDGET_BUSINESS.com.ju.demo.business" />
            </intent-filter>
        </service>

    </application>

</manifest>